# Concord
Software systems often ship with subtle but dangerous inconsistencies between what they claim to do (documentation) and what they actually enforce (tests). This project is a consistency checker designed to be run as a final, quick verification step before shipping.
